#It should be configured as follows with out any sparating space between value and name pair, only equal sign
# between them. There should be a space or newline between pairs. This file should not contain more than 1000
# characters.

server=localhost
database=ss_hss
username=root 
password=1234

